spring:
  application:
    name: request-service

  datasource:
    url: jdbc:postgresql://localhost:5432/gophermarket
    username: gopher
    password: market123

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: requests

  flyway:
    enabled: true
    schemas: requests,transactions
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8088
